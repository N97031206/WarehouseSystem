@model WarehouseSystem.Models.ViewModel.PalletTypeViewModel

@{
    ViewBag.Title = "PalletTypeEdit";
}

<script>
    //function onUploadFile(id) {
    //    // virtually trigger input file button
    //    $(id).trigger('click');
    //};

    //// // invoked by <input> tag upload photo file
    //function uploadPhoto(obj) {
    //    var err_logo_filetype = '只能上傳 png, jpeg 檔';
    //    //alert(typeof(obj.files[0].type));
    //    var imageType = ['image/png', 'image/jpeg'];
    //    if ($.inArray(obj.files[0].type, imageType) < 0) {
    //        alert(err_logo_filetype);
    //        return;
    //    }
    //    $('#photoProgress').css('width', '0%').html('0%');

    //    var formData = new FormData();
    //    formData.append('upload', obj.files[0]);
    //    // ajaxUploadFile('\{$homePath\}controller/cartController.php?' + $.param({'cartTypeId': '\{$cartTypeData[0]['cartTypeId']\}', 'action': 'updateCartTypePhoto'}), formData);
    //};

    //function ajaxUploadFile(url, formData) {
    //    $.ajax({
    //        url: url,
    //        type: 'POST',
    //        contentType: false,
    //        processData: false,
    //        cache: false,
    //        data: formData,
    //        dataType: 'json', // return JSON datatype
    //        xhr: function () {  // Custom XMLHttpRequest
    //            var myXhr = $.ajaxSettings.xhr();
    //            if (myXhr.upload) { // Check if upload property exists
    //                // show progress bar when uploading photo, 'progress' is reserve word, do not change it
    //                myXhr.upload.addEventListener('progress', function (e) {
    //                    var progress = parseInt(e.loaded / e.total * 100, 10);
    //                    $('#photoProgress').css('width', progress + '%').html(progress + '%');
    //                }, false); // For handling the progress of the upload
    //            }
    //            return myXhr;
    //        }
    //    }).done(function (data) {
    //        //alert(data.mapName);
    //        // update image
    //        $('#photo').attr('src', '~/Images/' + data.photoName + '?timestamp=' + new Date().getTime());
    //        $('#photoProgress').fadeOut('slow');
    //    }).fail(function () {
    //        alert("Sorry. Server unavailable. ");
    //    });
    //};

    $.fn.editable.defaults.mode = 'inline';

    $(document).ready(function () {
        // x-editable for user table
        $('#TypeName, #TypeCode').editable({
            ajaxOptions: {
                dataType: 'json' //assuming json response
            },
            success: function (response, newValue) {
                $('#LastUpdateTime').html(response['Result'].LastUpdateTime);
            }
        });
    });
</script>

<div class="wrapper">
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <h1>棧板管理<small>棧板類別編輯</small></h1>
            <ol class="breadcrumb">
                @*<li><a href="#"><i class="fa fa-dashboard"></i>Home</a></li>
                <li class="active">棧板管理</li>*@
            </ol>
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="row">
                <!-- left column -->
                <div class="col-md-12">
                    <!-- general form elements -->
                    <div class="box box-success">
                        <div class="box-header with-border">
                            <h3 class="box-title">編輯棧板類別</h3>
                        </div><!-- /.box-header -->

                        <div class="box-body">
                            <table class="table table-striped table-hover table-bordered" cellspacing="0" width="100%">
                                <thead>
                                    <tr>
                                        <th class="col-md-4">屬性</th>
                                        <th class="col-md-8">設定值</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr><td>類別名稱</td><td><a href="#" id="TypeName" data-type="text" data-pk="@Model.PalletType.PalletTypeID" data-url="@Url.Content("PalletTypeUpdate")" data-title="輸入名稱">@Model.PalletType.TypeName</a></td></tr>
                                    
                                    <tr><td>類別代號</td><td><a href="#" id="TypeCode" data-type="text" data-pk="@Model.PalletType.PalletTypeID" data-url="@Url.Content("PalletTypeUpdate")" data-title="輸入代號">@Model.PalletType.TypeCode</a></td></tr>
                                    @*<tr>
                                        <td>示意圖<input id="photoUpload" type='file' onchange="uploadPhoto(this)" style="display: none"><div class="btn btn-sm btn-success" onclick="onUploadFile('#photoUpload')"><i class="fa fa-map"></i> 上傳圖示</div></td>
                                        <td>
                                            <div class="progress" style="margin-bottom: 10px;">
                                                <div id="photoProgress" class="progress-bar progress-bar-success progress-bar-striped active" role="progressbar" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="width: 0%"></div>
                                            </div>
                                            <div><img id="photo" class="img-responsive" src="~/Images/@item.IconName" /></div>
                                        </td>
                                    </tr>*@
                                    <tr><td>建立時間</td><td>@Model.PalletType.CreateTime</td></tr>
                                    <tr><td>最新異動時間</td><td id="LastUpdateTime">@Model.PalletType.LastUpdateTime</td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div><!-- /.box -->
                </div>
            </div>
        </section><!-- /.content -->
    </div><!-- /.content-wrapper -->
</div><!-- ./wrapper -->